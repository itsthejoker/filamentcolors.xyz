<button
  type="button"
  class="btn btn-secondary position-relative {% if start_hidden %}d-none{% endif %}"
  {% if id %}id="{{ id }}"{% endif %}
>
  <span style="font-size: 24px" class="icon-arrow-left align-middle"></span>
</button>
